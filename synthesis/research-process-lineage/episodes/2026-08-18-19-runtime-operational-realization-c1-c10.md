# Research Episode — Runtime Operational Realization C1–C10

## Status

- episodeRole: `manual-curated-research-process-memory`
- ownerAuthority: `research-owner:runtime`
- ownerAuthorityVersionRef: `sha256:9c67d1b4094ce85a2465579430bb1a941f1923457087fb74cde0642d7b9a51b3`
- ownerSourceRevision: `50e29e68e99a274fe9b7819aa1bdbd27ec63cd38`
- currentStanding: `PROGRAMME_SATURATED_CURRENT_EVIDENCE_HORIZON`
- exhaustiveClosure: `UNKNOWN`

This episode preserves the epistemically load-bearing transitions of Runtime C1–C10. It is not Runtime owner truth and does not reproduce the conversation transcript.

## Initial question

After the Runtime Foundation corpus had been consolidated into six owner-native families and strong local saturation had been reached, the programme asked:

> Does real engineering consumption expose a concrete falsifier that requires Foundation repair, or can the frozen theory survive operational dogfood without additive Foundation research?

Initial discipline:

`Theory -> Engineering consumption -> Dogfood/falsification -> repair only when required`.

## Transition 1 — C1 recency rule repair

### Initial position

A first currentness probe treated:

`publication.sourceRevision != Git HEAD -> SOURCE_ADVANCED_STALE`.

This was mechanically tempting because Git revision is easy to observe.

### Trigger

Research System authority/currentness semantics and live Runtime owner transitions showed that Git recency does not mint/revoke semantic authority.

### Revision

Currentness was split into typed roles:

```text
Host continuity
transport observation
owner AuthorityVersion currentness
publication source/support horizon
```

### Surviving insight

`HistoricalValidity != CurrentStanding`.

The failed recency shortcut became an anti-rediscovery negative result.

## Transition 2 — C2 retry stops being error handling

### Initial pressure

A Git push timed out without a success acknowledgement.

### Rival simplifications

- timeout means effect failed -> retry;
- timeout means effect may have succeeded -> never retry.

### Hostile evidence

Authoritative remote observation established whether the target ref advancement was actually absent.

### Revision

Retry became a new admission decision after re-grounding:

```text
EffectKnowledge
+ OperationStanding
+ OperationContract
```

### Surviving insight

`Recovery != Rerun` and local process outcome cannot substitute for external effect knowledge.

## Transition 3 — C3 computational-economy hypothesis narrows itself

### Initial hypothesis

`ComputeNeed = UnsatisfiedSupportGap`.

### Hostile case

Exact replay of the same durable Operation reused the original realization with no new Attempt, but a distinct Operation with the same operationDigest still executed freshly. Changing source input changed the bound operationDigest.

### Revision

The hypothesis narrowed to:

`FreshExecutionNeed = UnsatisfiedAdmissibleSupportGap`.

Admissibility requires identity/binding/input/scope/evidence/standing compatibility.

### Surviving insight

Exact replay is established; generic cross-Operation semantic equivalence is not. Any future generic reuse likely needs an explicit Runtime↔SCD support/discharge boundary.

## Transition 4 — C4 exports Standing / Currentness

### Question

Was C1 currentness only a Runtime-repository artifact?

### Heterogeneous test

Network reproduced the same historical-valid/current-stale relation inside a shared physical research corpus, while local Git checkout, remote Git tip, owner CURRENT and publication source fence diverged.

### Revision

The abstraction was judged genuinely cross-owner.

### Result

`Standing / Currentness Semantics = PROMOTION_RECOMMENDED` as an independent research-programme candidate.

### Surviving owner boundary

Runtime retains Runtime-owned lifecycle standing. Cross-owner standing/currentness is not reabsorbed into Runtime simply because Runtime consumes it.

## Transition 5 — C5 support stops being scalar

### Initial simplification under attack

Strong evidence can be ranked on one scale; enough strong evidence should support a stronger claim.

### Hostile composition

Runtime terminal evidence + Host continuity + owner publication + Atlas projection still could not establish `HOST_GOAL_SUCCEEDED` or `WORLD_TRUE` without the corresponding typed owner bridge.

### Revision

Support became typed/multidimensional rather than scalar evidence strength.

### Surviving insight

Every semantic lift requires an explicit support bridge of the relevant type.

## Transition 6 — C6 global success stops being atomic

### Initial simplification under attack

A publication pipeline can be summarized by one success state.

### Real trace

The pipeline produced distinct legitimate states:

- local candidate exists but push lease rejects publication;
- owner authority current while Atlas projection remains stale;
- later Atlas refresh converges.

### Revision

End-to-end completion is a supported composition of scoped stage claims and bridges, not evidence for one cross-system transaction.

### Surviving insight

`GlobalSuccess != Sum(LocalSuccess_i)`.

## Transition 7 — C7 corrects the programme's own explanatory shorthand

### Earlier shorthand

C2 described one higher-level publication Operation with A1/A2 for clarity.

### Trigger

C7 read the exact durable Runtime ledger.

### Discovery

HTTPS push, reconciliation, SSH push and final verification were distinct Runtime Jobs/operationDigests/Attempts under one higher-level publication obligation.

### Revision

```text
SameHigherLevelObligation
!= SameRuntimeOperation
!= SameAttempt
```

Retry-like continuation may cross Runtime Operation identity when binding/provider/transport changes.

### Causal repair

A2 Runtime success alone did not prove external causation; post-state observation alone gave only association. Bounded attribution required pre-state absence + A2 transition evidence + post-state presence.

### Surviving insight

The programme preserved and corrected its own earlier simplification rather than retrospectively rewriting it as if the distinction had always been explicit.

## Transition 8 — C8 orthogonality receives live evidence

### Question

Can capability, authority, binding, standing and realization be collapsed operationally?

### Evidence

Live Workstation observations found:

- `surf-clash`: capability/generation identity observable while standing `UNAVAILABLE` and service/transport absent;
- `finance-okx`: stable profile/authority identity and configured members while no eligible member/listener/active realization existed.

Existing Workstation regression also preserved `AVAILABLE` candidates that still require admission/owner revalidation.

### Result

```text
Capability != Authority
Authority != Realization
Binding != Realization
Identity continuity != positive standing
```

### Surviving insight

Substitution is a fresh admission question, not history rewriting.

## Transition 9 — C9 destructive lifecycle dogfood remains scope-disciplined

### Fixture

A disposable Workspace produced a Job/artifact and was then exact-digest closed with physical removal.

### Observation

After removal:

```text
workspace usable = false
closure identity/history retained = true
Job/Attempt history retained = true
artifact bytes retained = true
```

Close replay returned `already_closed`; new execution failed before admission.

### Result

`PhysicalReclamation != HistoricalEvidenceErasure`.

### Important non-result

The first close combined closure and physical removal, so the experiment did **not** independently observe a temporally distinct retired-but-not-yet-reclaimed Workspace phase.

### Surviving methodological insight

Do not convert a theoretically useful distinction into an “observed fact” when the fixture did not separate the phases.

## Transition 10 — C10 stops the programme

### Pressure

After every six-family axis had received direct dogfood, continuing C11 by inertia would recreate the additive-research pattern the programme was designed to prevent.

### Hostile composite

C1–C9 component classes were combined into a scenario requiring simultaneous representation of ambiguity, retained history, stale/current authority, substitution, reconciliation, bounded causation and downstream staleness.

No cross-family contradiction appeared.

### Deletion audit

Removing any one six-family semantic axis caused a previously observed real semantic loss.

But the audit did not claim the exact six-folder taxonomy was uniquely minimal.

### R1–R8 audit

No Foundation reopen trigger survived.

### Current result

```text
PROGRAMME_SATURATED_CURRENT_EVIDENCE_HORIZON
WholeRuntimeExhaustiveClosure = UNKNOWN
Foundations remain frozen
No automatic C11
```

## Current surviving model

```text
Operational legitimacy
  = current + authority-compatible + identity-preserving
    + evidence-supported + correctly composed
    + open-world re-grounded + lifecycle-standing-aware
    + admitted realization
```

This is a synthesis compression, not a new Foundation.

## Superseded / rejected shortcuts worth remembering

- Git recency as semantic currentness;
- timeout/error as direct retry authorization;
- operationDigest equality as generic semantic reuse;
- scalar evidence strength as claim sufficiency;
- one global publication success bit;
- retry necessarily means same Runtime Operation;
- post-state observation alone as causation proof;
- capability/authority/realization collapse;
- reclamation as historical erasure;
- programme saturation as exhaustive closure;
- automatic numbered continuation after saturation.

## Durable provenance

Runtime:

- source closeout `50e29e68e99a274fe9b7819aa1bdbd27ec63cd38`
- current owner publication `sha256:9c67d1b4094ce85a2465579430bb1a941f1923457087fb74cde0642d7b9a51b3`
- publication transport `256ef6d0080de38c028a70e27cbaa08e0f817edb`
- `research/programme/PROGRAMME-SATURATION-CLOSEOUT.md`
- C1–C10 files under `research/programme/consumption/`
- Host `task:runtime-operational-realization-programme-20260818@rev13` completed

Atlas:

- synthesis collection `synthesis/2026-08-19-runtime-operational-realization/`
- live refresh after owner publication verifies the C1–C10 result `CURRENT_TO_SOURCE`.

## Anti-rediscovery standing

Future Runtime research should consult this episode and the owner programme closeout before admitting a route that resembles any C1–C10 transition.

A new theory route requires a surviving concrete falsifier, not a paraphrase of an already-settled shortcut.
