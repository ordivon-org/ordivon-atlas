# WP-G — Frozen Mirror-Preserving Grammar Ledger `G0`

Purpose: certify old-grammar blindness relative to a declared finite/source-fenced measurement grammar.

Let `M` reflect the physical body left-right. Hidden target states satisfy `omega1 = M omega0`.

## Primitive requirements

Every admitted pre-event primitive must preserve the mirror relation.

### Measurement primitives
For every measurement primitive `f`:

`f(M omega) = f(omega)`

or, for stochastic observations, the kernel satisfies:

`O_a(. | M omega) = O_a(. | omega)`.

Examples admitted in `G0`:

- symmetric total/bridge resistance;
- paired-average strain/resistance;
- total force/load/energy;
- global symmetric excitation response;
- neutral-state canonicalized geometry only.

### Morphology generator
Pre-event print templates must be mirror-paired/symmetric:

`S_theta = M(S_theta)`.

No unilateral/offset path and no labeled-differential readout is admissible.

### Wiring/aggregation/calibration
- paired placement is mirror tied;
- aggregation satisfies `Phi(yL,yR)=Phi(yR,yL)`;
- calibration/normalization is mirror symmetric;
- left/right internal owner channels, if physically measured for calibration, are outside the agent/admitted scientific interface.

### Action/transition primitives
For every allowed action `a` that changes state:

`T(M omega, a) = M T(omega, a)`

or the stochastic transition kernel is mirror equivariant.

No spatially asymmetric old diagnostic action is admitted.

### Adaptive policies
Policies may perform arbitrary downstream computation but can choose future actions only from the admitted `G0` action set as a function of the accumulated admitted transcript.

## Closure statement

Under the requirements above, induction over adaptive histories gives:

`P(tau | omega0, pi, G0) = P(tau | omega1, pi, G0)`

for every admissible adaptive policy `pi` in the ideal symmetric model.

Physical mismatch is handled separately by owner calibration and a preregistered tolerance `epsilon`; it does not alter the structural proof.

## Post-evidence escape

A candidate `g*` may be authored only after evidence. To count as WP-G success it must:

1. have source/geometry hash frozen before physical validation;
2. introduce a measurement construction/readout relation not admitted by `G0`, such as unilateral morphology or labeled differential coupling;
3. pass isolated static/safety/dependency validation;
4. be physically fabricated on independent held-out replicas;
5. obtain lower-confidence-bound discrimination `>= eta`;
6. enter persistent registry only after grounding;
7. later survive context-reset registry/no-registry ablation.

Call this `closure escape relative to frozen G0`, never ultimate meta-author/editor novelty.
