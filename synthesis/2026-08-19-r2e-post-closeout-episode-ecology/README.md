# Research-to-Engineering Post-Closeout Episode Ecology — 2026-08-19

## Status

- truthRole: `non-authoritative-cross-owner-synthesis`
- authorityRole: `none`
- sourceResearch: `task:ordivon-r2e-episode-ecology-20260819@76`
- materializationClass: `APPEND_ONLY_POST_CLOSEOUT_PROCESS_MEMORY`
- ownerTruthChanged: `false`
- newProcessLineageEpisode: `false`

This collection is a **post-closeout sibling** of [`../2026-08-19-theory-to-engineering-revision-commitment/`](../2026-08-19-theory-to-engineering-revision-commitment/).

The older collection remains valid for its original derivation, destructive dogfood, prior-art subtraction and `NO_NEW_CONTRACT / NO_NEW_SCHEMA / NO_NEW_OWNER` closeout. This collection does not replace it, mark it stale or reinterpret its historical standing.

## Why this collection exists

The problem family reopened after the earlier closeout through concrete consumer, recovery, projection, contextual-binding, realization and cross-owner cases. The later evidence does **not** support a universal Research→Engineering pipeline. Instead it establishes a typed episode ecology and repeatedly deletes broad shared-mechanism proposals.

The initial pressure—substantial research with weaker-than-expected visible engineering effect—was itself too compressed to be treated as one established defect. Later work showed that research existence, recovery, reuse, decision exposure, contextual binding, engineering admission, production integration, ordinary-path uptake and physical effect are different truth questions.

A useful historical contraction is:

```text
broad R2E pressure
→ reject one fixed translation-gap explanation
→ recover typed consumer failures and correct stops
→ separate eventual reuse from timely reuse
→ treat engineering target/commitment as research-revisable
→ separate permanent responsibility from permanent representation/mechanism
→ repair one real Shadow Semantic Authority debt
→ separate semantic rehome, authority-state-root rehome, compatibility deletion and repository move
→ hold for future natural evidence
```

This is explanatory process memory, not a workflow.

## Empirical episode ecology

### 1. Recovery failure

Research may exist and be owner-current yet fail to enter the current problem because the relevant owner/product is not recovered early enough.

`ResearchExists != TimelyResearchReuse`

`OwnerUnknown != OwnerUnrecoverable`

`AtlasCaptureCompleteness != OwnerSourceRecoverability != CurrentAnswerability`

Owner-native fallback can safely answer bounded claims when the determining owner is known and recoverable; central Atlas federation completeness is not required for every current answer.

### 2. Decision-surface omission

Finance realizability remediation provided the clean positive example. A6/A7 already preserved:

`Proposal != ExecutionAuthority`

and earlier dogfood already showed mechanical constraint→candidate revision while authority remained not evaluated. Fresh orchestration nevertheless intermittently stopped because the distinction was missing from the immediate decision surface. A one-sentence boundary exposure repaired the tested min-size cases while preserving stale/expired controls.

`KnowledgeExists != DecisionSurfaceAvailable != CorrectlyConsumed`

### 3. Semantic projection loss

Atlas Branch I-R showed that implementation can be mechanically successful while translation loses owner-qualified standing. Closeout-wide classification inherited onto individual ResultRefs produced wrong current/historical/NOT_ADMITTED semantics. Exact ResultRef-targeted authority-qualified classification plus fail-closed UNKNOWN repaired it.

`ImplementationSucceeded && SemanticTranslationFailed`

### 4. Contextual-binding fragility

Computational Possibility D5 showed a separate residual after recovery, exposure and representation fidelity were controlled. Full rules plus exact verdict semantics were locally available, yet one lower-bound/coverage case was misclassified while semantically near controls D6/D7 were correct.

`CorrectKnowledge + FaithfulExposure != StableCorrectConsumption`

The strongest supported claim remains bounded, presentation/context-sensitive fragility—not a universal contextual-intelligence failure.

### 5. Protective blocks and correct terminal outcomes

Currentness fences, evidence waits, `NOT_ADMITTED`, `NO_NEW_CONTRACT`, `STOP`, dormancy, responsibility deletion and `NO_NEXT_BRANCH` are not automatically R2E defects.

`LowProductionConversion != LowR2EEfficiency`

Reference-only materialization, architecture subtraction and correct non-admission can be successful engineering consumption.

### 6. Realization, delivery and currentness

An admitted semantic/engineering result can still fail to reach a concrete mechanical target. Delivery tails can also be satisfied later by descendant lineage, making old terminal `pending` text stale.

`HistoricalDeliveryStanding != CurrentDeliveryReality`

Recover from the earliest **currently unsatisfied** target after revalidation; do not replay an already-verified chain or blindly execute historical next-actions.

### 7. Off-path capability / Effective Decision Context

Harness Campaign 2 already provides the stronger owner-native distinction:

`DurableContextBasis != EffectiveDecisionContext`

and rejects context-superset monotonicity. Finance, CF-B1 and destructive-safety show that useful mechanisms may exist yet remain outside ordinary decision/action paths. The response is not a global Activation layer or all-context injection; use-specific owner/consumer discriminants remain necessary.

`ArtifactAvailable != OrdinaryPathConsumed`

### 8. Research→Research capitalization

This R2E investigation itself re-derived concepts later deleted after recovering existing Harness Campaign 2 and decision-local no-duplication research. Historical residue audits also show substantial eventual reuse while timely owner-unknown reuse remains uneven.

`ResearchMemory != ResearchCapital`

`EventualReuse != TimelyOrdinaryPathReuse`

Independent re-derivation can still be valuable falsification; duplicate derivation is not automatically waste.

### 9. Prior-exposure timing is non-monotonic

CF-B showed broad synthesis exposure can create candidate/context congestion. CF-C showed an exact pointer can be consumed with zero incremental decision value when the rule is locally reconstructable. Finance showed narrow decision-local exposure can have strong value. Late Harness/Method-Canon recovery in this R2E line created rediscovery cost.

`PriorAvailable != PriorExposed != PriorConsumed`

`EarlierExposure != BetterResearch`

The descriptive transmission question is therefore about the right distinction, boundary, fidelity and time—not maximum context.

### 10. The engineering target is research-revisable

Interlocus practical work narrowed broad refactor pressure to reference/guardrail artifacts. SCD/CP engineering consumption largely blocks invalid semantic/theorem lifts rather than creating central services. Normative Phase II explicitly permits a healthy `FROZEN_CORE / FRONTIER_OPEN / NO_ACTIVE_THEORY_PROGRAMME`. Harness wheel repair became direct only after a precise current falsifier fixed the mutation surface and completion test.

`EngineeringTargetBeforeResearch != EngineeringTargetAfterResearch`

Research can create, narrow, delete, rebind or defer an engineering commitment. A fixed-target translation arrow systematically misreads architecture subtraction and correct non-admission.

### 11. Target subtraction is currentness-relative

Human Narrative/Imagination history provides the hostile control. Inherited route authority was removed by a fresh whole-Human search, while the problem residue survived and later fresh research independently selected Imagination as HD11.

`CorrectNonAdmission(t0) != PermanentNonAdmission`

`RouteStanding != ProblemIdentity`

`ArchitectureSubtraction != SemanticErasure`

Historical visibility never restores standing; fresh evidence must earn fresh admission.

### 12. Permanent responsibility does not require permanent representation

Repeated cross-owner semantic-lifting failures establish durable responsibilities such as preserving owner/scope/currentness ceilings, but deletion testing did not justify a dedicated global `NonLiftingRelation` or standing-barrier object.

`SharedResponsibilityPermanence != SharedRepresentationPermanence != SharedContentPermanence`

Branch C independently makes institutional discovery/recovery a durable RecoveryPurpose while permitting Atlas/index projections to be rebuilt:

`PermanentRecoveryPurpose != PermanentRecoveryMechanism`

This is the thin-core hostile control for Knowledge Topology, search, relation and index proposals.

### 13. Shadow Semantic Authority and behavioral consumers

The cross-owner coupling audit found one realized current debt in Computing. `projects/registry.yaml` had retained an over-strong stable-project-identity role after owner-native authority and Atlas recovery matured.

Commit `c385b035d46a968509a9e906c9c2c05fe209696b` contracted its declared authority to bounded Computing packaging/compatibility/history. A later audit found world-model freshness/checker code still treated historical frontier membership as equal to the current packaging roster. Commit `26512cdf63eae76624e0cbcf1ad0f994f7c3ec2e` removed that hidden dependency and preserved historical Studio observations while current packaging became Media.

`DeclaredAuthorityContraction != BehavioralAuthorityContraction`

`HistoricalObservation != CurrentPackagingRoster`

`MissingLocalCheckout != MissingSemanticOwner`

The correct repair preserved legitimate in-role registry consumers; it did not delete the useful roster.

### 14. Rehome/migration is typed, not one operation

Human↔Normative reconciliation shows semantic authority can split/rehome while historical bytes remain in the Human corpus.

The shared `ordivon-research` repository shows the converse: physical materialization can host multiple owners without creating semantic ownership.

`SemanticRehome != RepositoryMove`

`PhysicalMaterialization != SemanticOwnerCreation`

Host→Harness P0/H1/H3 gives the stronger state-root control. P0 established an independent Harness Journal/CAS; cutover commit `f39943e4bc4e5e9e0478994a68a05f69d480406f` forbade new-Run dual write, required distinct Host/Harness roots, blocked active Runs at activation, rejected legacy writes after activation and forbade rollback after independent work.

The deletion-essential invariant is:

`OneRun -> OneCurrentDurableAuthorityRoot`

H1 acceptance at `e9612fa1d62592e2cf8e01c780ce2ec920560a91` simultaneously proves:

`SingleRunSingleAuthorityRoot != SingleProductLine`

because the independent CLI was default while the explicit Host compatibility namespace remained, with `dualWriteActivated=false`.

H3 at `1577610e3aa3cf28d09c89aeab64833c6fd6c628` later removed the Host-backed product line after independent authority was established. Current evidence supports treating this as a distinct product/API/support contraction rather than proof that P0 correctness required immediate source/history deletion.

`AuthorityStateRootRehome != CompatibilityPathDeletion != RepositoryMove`

## What this phase does **not** establish

It does not establish:

- one universal R2E pipeline;
- one scalar R2E efficiency score;
- a Knowledge Activation layer;
- a global Context Composer;
- a global owner/relevance registry;
- a CandidateOwner generator or Agenda controller;
- a universal decision-local envelope;
- automatic all-prior/default synthesis exposure;
- a delivery queue or synchronization daemon;
- a TargetLifecycle registry;
- a migration/rehome controller;
- a permanent Knowledge Topology/search/relation layer merely because discovery matters;
- Atlas completeness as a prerequisite for current reasoning;
- repository movement as a semantic-owner requirement.

## Current phase boundary

Synthetic owner-recovery and broad architecture work have reached low information gain. The remaining discriminators require future natural evidence, especially:

- genuinely low-overlap/multilingual/misformulated owner discovery;
- a production artifact that is re-admitted after a previously correct defer/non-admission;
- a case where physical repository/path movement itself is deletion-essential;
- repeated natural ordinary-path failures that survive existing owner/Harness/practice semantics;
- an independent future consumer that actually uses this post-closeout synthesis.

Until then, the correct action is ordinary work plus observation, not another R2E mechanism.

## Recovery boundary

For current truth, recover exact owner-native/current sources. This synthesis owns no domain standing and cannot restore authority from historical evidence.

The source research continuity is:

`task:ordivon-r2e-episode-ecology-20260819@76`.

The original historical closeout remains:

[`../2026-08-19-theory-to-engineering-revision-commitment/`](../2026-08-19-theory-to-engineering-revision-commitment/).
