# Anti-Rediscovery Guard — Theory-to-Engineering / Finite-Intelligence Route

## Read this before reopening the route

This file exists because the conversation generated several attractive high-level abstractions that were later destructively contracted or rejected. Future Agents should not reconstruct them from scratch and mistake conceptual recurrence for new standing.

## Closed / rejected routes

### 1. `Praxis` as a new Ordivon owner

**Do not recreate by default.**

The research-to-engineering problem did not demonstrate an owner-native semantic object or deletion-essential operational authority that belongs to a new Praxis owner.

Current owner topology remains distributed across Research System, Normative, Interlocus, Harness, Runtime/engineering owners, Computer synthesis and Atlas projection/recovery.

### 2. `Finite Situated Intelligence Dynamics` as an independent theory

**NOT ADMITTED.**

The D1–D9, K0–K3 and F0–F3 structures remain historical derivation/compression only. Component mechanics have strong mature ancestors and the umbrella failed deletion-essentiality against real Ordivon cases.

Do not reopen because the equations are elegant or the coverage is broad.

### 3. Every anomaly as `Representation Escape`

**Rejected by HD8 and stale-ready controls.**

Large judgment changes can result from evidence/contact fidelity. Historical READY/current mismatch can be a currentness reconciliation. Capability/admission separation can be an action gate with no ontology revision.

Always preserve:

`FailureLocus != RevisionDepth`.

### 4. Universal Standing schema/table/registry

**REJECTED.**

Research System Minimal Contract already models standing as composable owner-qualified statements and currentness separately. Normative, capability, authority and epistemic roles must keep owner-native semantics.

Do not normalize them into one universal enum, scalar confidence or utility.

### 5. Persistent `DecisionLocalStandingEnvelope`

**REJECTED at current consumer standing.**

A decision may derive an ephemeral read view over exact source refs, but making that view persistent would duplicate owner truth/currentness, create another provenance surface and risk shadow authority.

### 6. `OperationalClaimUseDisposition` by symmetry

Harness already contains the nearest bounded use-projection candidate, but current Harness standing rejects implementing it without a real deletion-essential consumer.

Do not materialize it merely to complete this synthesis aesthetically.

### 7. Technical repair sufficiency as recommendation/permission

Interlocus Counterfactual Repair explicitly rejects:

`technical sufficiency -> action recommendation`.

Keep separate:

- technical target restoration;
- protected constraints;
- normative/action admission;
- preference/optimality;
- actual post-action evidence.

### 8. Synthetic destructive-safety fixture as proof of historical safety causation

The fixture supports `Capability != Admission/Commitment` mechanism feasibility.

It does **not** prove why no known catastrophic unrecoverable loss occurred historically. Rival causal explanations remain live unless independently adjudicated.

### 9. v1 prospective triage as clean predictive validation

v1 input leaked owner frontier/next-action semantics. It remains an open-label calibration dataset only.

Do not count `NextOperator` matches as incremental predictive evidence.

Only a blind-input v2-style programme can establish stronger prospective value.

## Durable survivors

The following may be reused as thin audit concepts without theory admission:

- `FailureLocus != RevisionDepth`;
- locate failure before choosing revision depth;
- minimal revision / Revision Selectivity;
- commitment should not outrun contemporaneously supported owner/context-typed standing;
- `UNKNOWN` may permit bounded inquiry but must not silently mint stronger commitment;
- Reality, representation/contact, standing and actual effect must not collapse;
- projection/read views do not become owner truth;
- programme closure does not mean Reality can never provide reopen evidence.

## Reopen triggers

A new research/engineering branch is justified only by concrete pressure such as:

- blind prospective triage materially beating `OWNER-LOCAL-FIRST`;
- an existing-contract representational failure that loses a decision-relevant distinction;
- a repeated real consumer that cannot preserve owner-native typed standing/currentness/provenance through current bridge surfaces;
- a destructive counterexample to the no-new-contract conclusion;
- materially new prior-art/Reality evidence invalidating the completed subtraction.

Do **not** reopen for:

- naming symmetry;
- conceptual elegance;
- a desire for one unified theory;
- an empty service/repository slot;
- ordinary bugs or stale currentness that owner-native mechanisms already solve.

## Recovery pointers

- `task:finite-intelligence-dynamics-destructive-dogfood-20260819@5`
- `task:revision-commitment-triage-theory-subtraction-20260819@5`
- `task:decision-local-typed-mediation-no-duplication-audit-20260819@3`
- `task:ordivon-research-system-minimal-contract-materialization-20260818@3`
- `task:ordivon-research-operating-discipline-v0-20260819@5`
- `task:interlocus-capability-path-explanation-dogfood-20260819@4`
- `task:interlocus-counterfactual-capability-repair-20260819@6`

If later owner-current material conflicts with this synthesis, owner-current material wins.