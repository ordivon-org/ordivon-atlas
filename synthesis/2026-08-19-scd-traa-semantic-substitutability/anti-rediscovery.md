# SCD TRAA — Anti-Rediscovery Guard

## First lookup

Before reopening this route, read in order:

1. `task:scd-traa-derived-theory-live-dogfood-20260819@5`
2. Computing commit `50c65fafb37a29b8f1c25a7ea2a92eff99c0b5de`
3. `research/core/semantics-of-computational-descriptions/TRAA-DERIVED-THEORY-CANDIDATE-LIVE-DOGFOOD-20260819.md`
4. this Atlas synthesis only for navigation/process context.

Do **not** reconstruct TRAA v0-v6 from conversation history.

## Already rejected or demoted

Do not re-propose without a new falsifier:

- primitive `Phi_T = all information needed for T`;
- universal `StateSpace` or replacement universal `TraceSpace`;
- equivalence-only adequacy;
- `SoundApproximation == Adequacy`;
- unary `ContextAdmissible(K)` or a universal context-set ontology;
- truth-role labels as sufficient Agent inference control;
- a generic "do not invent bridges" rule as universal control;
- typed intermediate targets alone as universal cross-model control;
- current-corpus feature correlation as constitutive semantic law;
- universal `CrossTruthRolePromotion` as an LLM law;
- a single `BridgeStanding` enum as the whole support-topology mechanism.

## Already established in scope

Do not rediscover these from scratch:

- exact projection/provenance does not establish semantic sufficiency;
- semantic soundness and obligation sufficiency are different;
- local adequacy need not survive context composition;
- generic functional similarity need not imply recovery-sensitive substitutability;
- support availability does not automatically establish target standing;
- model success/accuracy cannot certify representation adequacy;
- semantic owner truth and model-specific cognitive reliability are different research objects.

## Do not inflate novelty

Generic warrants, argument graphs, proof obligations, provenance, semantic roles, proof-carrying actions, graph contracts and evidence-force calibration already have mature prior art. Any future novelty claim must survive direct comparison with those bodies of work.

## Open, not settled

The following remain research questions:

- whether target-scoped support closure belongs inside TRAA or is a separate SCDF9-derived theory consumed by it;
- whether `EXTERNAL_REQUIRED` reduces to `NOT_ESTABLISHED + external discharge dependency`;
- whether the narrow heterogeneous-owner semantic-substitutability residual is genuinely novel;
- how positive bridge standing should carry authority/currentness/applicability;
- whether TRAA remains a useful named programme after tighter theory subtraction.

`Open != automatic next route`. Reopen only with a concrete falsifier, consumer failure, or literature result.
