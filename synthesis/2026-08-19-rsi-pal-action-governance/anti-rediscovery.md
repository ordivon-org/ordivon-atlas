# Anti-Rediscovery Guard — RSI / PAL Action and Governance Synthesis

## Purpose

Before reopening this conversation chain, recover these already-earned negative results and distinctions. This file is navigation/synthesis only, not owner truth.

## Do not silently re-collapse

- `RealResponsibility != MissingResponsibility`; candidate responsibility fit is not enough without marginal gap closure.
- `Centralization != AuthorityInflation`; central or distributed authority must match the underlying responsibility/truth topology.
- `Evidence != Claim != Standing != UseDisposition != ActionPermission`.
- `ActionAdmission != ActionSelection != Settlement != Commitment != Effect`.
- `PremiseUncertainty != PremiseBlindness`.
- `DeclaredInputs != CompleteCausalDependencyClosure`.
- `Compensation != Rollback`; `Cancel != Rollback`; `MoreSteps != MoreReversibility`.
- `Risk != Ambiguity != StructuralUncertainty`.
- `RobustOptimization != RobustToUnknownStructure`.
- `LocalEffectBound != CausalReachBound` and `ActionBounding != PropagationBounding`.
- `CausalReach != CausalAmplification`; `Propagation != Gain`.
- `ReachGain != MagnitudeGain != GenerativeGain`.
- `Cycle != Source`; `Persistence != Gain`; `InformationReplication != AgencyReplication`.
- `Capability != Authority`; `Performance != Authority`; `Influence != Legitimacy`; `RepresentationCausalPower != RepresentationTruth`.
- Causal capacities are typed/non-fungible; conversion requires an explicit bridge.
- `RuleUsePower != RuleChangePower`; physical rule-file mutation does not establish valid rule change.
- A new admission rule must not become the sole basis for admitting the same transition that creates it.
- Incumbent challenge monopoly and challenger self-admission are both failure modes.
- `IncumbentFailure != ChallengerValidity`; `Exit != Replacement`; `Replacement != Succession != Genesis`.
- `ManyGrounds != OneUtilityFunction`; `GroundComposition != GroundAggregation`.
- `Settlement != Resolution`; `SelectionClosure != ReasonClosure`.
- `DecisionFinality != TruthFinality != EffectFinality != HistoricalFinality`.
- `Irrevocability != Legitimacy`; `Appeal != DecisionInvalidity`; `InquiryFinality != TruthCertainty`.

## Do not over-promote candidate labels

The conversation used several useful compression handles. None is automatically an admitted theory/owner/Foundation/service:

`Standing-Matched Commitment`, `Authority Topology Fidelity`, `Action Blind Spots`, `Commitment Surface`, `Causal Gain`, `Causal Capital`, `Gain Conversion`, `Conversion-Rule Change`, `Constitutional Escape`, `Non-Sovereign Governance`, `Procedural Finality`.

If a future route uses one of these names, first re-run prior-art / owner-boundary / deletion tests and recover the exact owner-native evidence.

## Next-frontier guard

The only deliberately selected continuation is **Agenda Power / Option-Space Governance**.

Do not automatically convert later residuals into a roadmap, Campaign, Foundation number, new owner, or implementation programme.
